# 修改记录

本文档记录 WACV 2027 版本相对 ECCV 权威版本的主要修改，便于后续回溯。

## 迁移原则

- 内容以 `../EDeflare_ECCV_2026/` 为准。
- `../CVPR2026_E_Deflare (2)/` 只作为双栏排版和图表紧凑性参考。
- WACV 主文需要满足 8 页限制，不含 references。
- 之前的 review 和 rebuttal 意见记录在 `rebuttal意见.txt`。

## 已完成的主要改动

### WACV 模板与元信息

- 将主文迁移到 WACV 2027 author kit。
- 设置投稿编号为 `744`。
- 使用 Algorithms track。
- 创建并维护主文 `main.tex`、补充材料 `supplementary.tex` 和 rebuttal 入口。

### 内容迁移

- 主文内容从 ECCV 版本迁移到 `sec/`。
- 图像、补充材料图像和 bibliography 同步到 WACV 目录。
- 保留 ECCV 版本中更完整的物理建模、PNL-ES 推导、benchmark 描述和实验结果。

### 排版修正

- 删除主文中间会导致大块空白的 `\FloatBarrier`。
- 在 `preamble.tex` 中加入 `stfloats`，允许双栏图表在页底出现。
- 实验节双栏图表使用 `[!tbp]`。
- 实验节单栏下游图表使用 `[!htbp]`。
- 将主实验表提前声明到 Experiments 开头，避免图表集中堆到后面形成纯图表页。

### 最近一次内容调整

- 隐藏主文 Table 1 数据集概览表，保留数据集文字列表和样例图。
- 将 `4.4 Downstream Task Evaluations` 缩写为只保留 event-based imaging 结果和图像重建图。
- 将 event-based 3D reconstruction 的表、图和说明移入补充材料 `Additional Experimental Results`。
- 调整 abstract 和 introduction 中关于 E-DeflareNet 的表述，不再把网络本身包装成核心贡献。当前表述明确 E-DeflareNet 是一个 standard 3D U-Net restoration network，用来验证物理公式和配套生成数据的有效性。

## 待处理事项

- 当前 clean-layout 版本主文约 9 页，需要继续压缩到 WACV 允许的 8 页。
- 后续优先考虑压缩 captions、Related Work 和部分方法细节，而不是回退到旧 CVPR 内容。
- 需要继续对照 `rebuttal意见.txt`，优先澄清 baseline 选择、真实数据采集限制、standard network 的定位，以及 DSEC-Flare 泛化证据。
